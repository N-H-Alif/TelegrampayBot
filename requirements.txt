# Telegram Bot API wrapper
pyTelegramBotAPI==4.18.0

# MongoDB driver (srv extra required for Atlas connection strings)
pymongo[srv]==4.7.3

# Background task scheduling
APScheduler==3.10.4

# Keep-alive web server for Render
Flask==3.0.3

# Production WSGI server (optional but recommended on Render)
gunicorn==22.0.0
